# deepagents-landlock

Landlock LSM sandbox backend for [LangChain Deep Agents](https://github.com/langchain-ai/deepagents).

Uses Linux kernel Landlock for local, non-privileged, kernel-enforced filesystem isolation.
